import React from 'react';

const Rule = () => {
    return (
        <div className={'rule-wrapper'}>
            <input className={'rule'} maxLength={2}/>
        </div>
    );
};

export default Rule;
